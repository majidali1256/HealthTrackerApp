<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Hardware Features -->
    <uses-feature android:name="android.hardware.sensor.stepcounter" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.heartrate" android:required="false" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <application
        android:name=".HealthTrackerApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HealthTracker"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="34">

        <!-- Splash Screen -->
        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.HealthTracker.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth Activities -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.HealthTracker" />

        <activity
            android:name=".ui.auth.SignUpActivity"
            android:exported="false"
            android:theme="@style/Theme.HealthTracker" />

        <!-- Main Dashboard -->
        <activity
            android:name=".ui.dashboard.DashboardActivity"
            android:exported="false"
            android:theme="@style/Theme.HealthTracker" />

        <!-- Profile Setup -->
        <activity
            android:name=".ui.profile.ProfileSetupActivity"
            android:exported="false"
            android:theme="@style/Theme.HealthTracker" />

        <!-- Profile -->
        <activity
            android:name=".ui.profile.ProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.HealthTracker" />

        <!-- Medical ID -->
        <activity
            android:name=".ui.profile.MedicalIdActivity"
            android:exported="false"
            android:theme="@style/Theme.HealthTracker" />

        <!-- Settings -->
        <activity
            android:name=".ui.profile.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.HealthTracker" />

        <!-- Facebook Login -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        <meta-data
            android:name="com.facebook.sdk.ClientToken"
            android:value="@string/facebook_client_token" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />

        <!-- Boot Receiver for Medication Reminders -->
        <receiver
            android:name=".receivers.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Medication Reminder Receiver -->
        <receiver
            android:name=".receivers.MedicationReminderReceiver"
            android:exported="false" />

        <!-- Step Counter Service -->
        <service
            android:name=".services.StepCounterService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="step_counting" />
        </service>

        <!-- Emergency SOS Service -->
        <service
            android:name=".services.EmergencySOSService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- Sleep Analysis -->
        <activity
            android:name=".ui.sleep.SleepAnalysisActivity"
            android:exported="false"
            android:theme="@style/Theme.HealthTracker" />

        <!-- Tools Hub -->
        <activity
            android:name=".ui.tools.ToolsActivity"
            android:exported="false"
            android:theme="@style/Theme.HealthTracker" />

        <!-- Document Vault -->
        <activity
            android:name=".ui.documents.DocumentVaultActivity"
            android:exported="false"
            android:theme="@style/Theme.HealthTracker" />

        <!-- Food Logger -->
        <activity
            android:name=".ui.food.FoodLoggerActivity"
            android:exported="false"
            android:theme="@style/Theme.HealthTracker" />

        <!-- Symptom Checker -->
        <activity
            android:name=".ui.symptoms.SymptomCheckerActivity"
            android:exported="false"
            android:theme="@style/Theme.HealthTracker" />

        <!-- Medications -->
        <activity
            android:name=".ui.medications.MedicationsActivity"
            android:exported="false"
            android:theme="@style/Theme.HealthTracker" />

        <!-- Trends -->
        <activity
            android:name=".ui.trends.TrendsActivity"
            android:exported="false"
            android:theme="@style/Theme.HealthTracker" />

        <!-- Medication Action Receiver -->
        <receiver
            android:name=".receivers.MedicationActionReceiver"
            android:exported="false" />

    </application>

</manifest>
